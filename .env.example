# ──────────────────────────────────────────────
# Quorum — Environment Variables
# Copy to .env and fill in values
# ──────────────────────────────────────────────

# ── PostgreSQL ────────────────────────────────
POSTGRES_USER=quorum
POSTGRES_PASSWORD=quorum_dev_password
POSTGRES_DB=quorum
DATABASE_URL="postgresql://quorum:quorum_dev_password@localhost:5432/quorum?schema=public"

# ── Zcash Network ─────────────────────────────
ZCASH_NETWORK=testnet

# ── Zebra Node (uncomment when P0-B1 is resolved)
# ZEBRA_RPC_HOST=localhost
# ZEBRA_RPC_PORT=18232

# ── Zaino Indexer (uncomment when P0-B1 is resolved)
# ZAINO_ENDPOINT=http://localhost:8080

# ── Next.js ───────────────────────────────────
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_ZCASH_NETWORK=testnet
